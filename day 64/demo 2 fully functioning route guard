



-------------------login.ts

  import { Component } from '@angular/core';
import { Router } from '@angular/router';

@Component({
  selector: 'app-login',
  standalone: false,
  templateUrl: './login.html',
  styleUrl: './login.css',
})
export class Login {

  constructor(private router:Router){

  }

  userName="";
  password="";

validateLogin(){

  if(this.userName=='admin' && this.password=='admin123')
  {
    alert('valid user')
    this.router.navigate(['home',this.userName,'testadmin']);
    localStorage.setItem('userType','admin')
  }
  else if(this.userName=='user' && this.password=='user123')
  {
    alert('valid user')
    this.router.navigate(['home',this.userName,'myuserdata']);
 localStorage.setItem('userType','user')

  }
  else
    alert('invalid user')

}

}

--------------------------paint-guard.ts

import { CanActivateFn } from '@angular/router';

export const paintGuard: CanActivateFn = (route, state) => {
let userType=localStorage.getItem("userType")

if(userType=='admin')
  return true;
else
  return false

};

-----------------app-routing.module.ts

import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';
import { Home } from './home/home';
import { Services } from './services/services';
import { Contact } from './contact/contact';
import { About } from './about/about';
import { Carwash } from './services/carwash/carwash';
import { Carrent } from './services/carrent/carrent';
import { Carpaint } from './services/carpaint/carpaint';
import { Login } from './login/login';
import { paintGuard } from './paint-guard';

const routes: Routes = [
{path:'',redirectTo:'login',pathMatch:'full'},
  {path:'home/:userId/:pass',component:Home},
  {path:'about',component:About},
  {path:'contact',component:Contact},
   {path:'login',component:Login},
  {path:'services',component:Services,
    children:[
      {path:'',redirectTo:'carwash',pathMatch:'full'},
       {path:'carwash',component:Carwash},
       {path:'carrent',component:Carrent},
       {path:'carpaint',component:Carpaint, canActivate:[paintGuard]}
    ]

  }
  


];

@NgModule({
  imports: [RouterModule.forRoot(routes)],
  exports: [RouterModule]
})
export class AppRoutingModule { }



--------------------------------- 

hit localhost:4200 the default url that opens a login page 
  -> login using admin user which is -username admin password admin123 
  -> try to access the service cappaint 


  hit localhost:4200 the default url that opens a login page 
  -> login using noraml user which is -username user password user123 
  -> try to access the service cappaint 
  you wont be abvle to use car paint as u r unathorised for it (that what we have configured in route guard)


  
  
  

  



